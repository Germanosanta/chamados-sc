{
  "name": "chamados-sc-scripts",
  "private": true,
  "version": "1.0.0",
  "description": "Scripts de manutenção pontual (fora do app publicado em docs/ — não entram no deploy do Hosting).",
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
